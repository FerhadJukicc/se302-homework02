import { test } from '@playwright/test';

test.describe('C10 - Add item to basket updates basket count', () => {
  test('Basket count increases after adding a product', async ({ page }) => {
    // TODO:
    // 1) Go to /sweets
    // 2) Click an "Add to Basket" action
    // 3) Assert basket count or basket page summary changes (use expect)
  });
});
