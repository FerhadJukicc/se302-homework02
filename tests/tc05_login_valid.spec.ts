import { test } from '@playwright/test';

test.describe('TC04 - Login accepts valid credentials', () => {
  test('User logs in successfully with valid email and password', async ({ page }) => {
    // TODO:
    // 1) Go to /login
    // 2) Fill email + password (example credentials are shown on the login page)
    // 3) Click login
    // 4) Assert expected post-login behavior (redirect or UI indicator) OR at minimum that credentials are not leaked
  });
});
